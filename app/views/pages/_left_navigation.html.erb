
	        	<%= if (@comic && @comic.id > 1)
	      		  link_to (image_tag "back.png"), comic_path(@comic.previous)
	  			elsif (@comic.nil? && Comic.last) #this is main page text
	  			  link_to (image_tag "back.png"), comic_path(Comic.find_by_id(Comic.last.id - 1))
	      		end %>
	      	

	      		<%= if (@comic && @comic.id > 1)
	      		  link_to (image_tag "first.png"), comic_path(Comic.first)
	  			elsif (@comic.nil? && Comic.last) #this is main page text
	  			  link_to (image_tag "first.png"), comic_path(Comic.first)
	      		end %>
	  			
