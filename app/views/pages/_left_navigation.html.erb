
	        <%= if( @comic.id > Comic.last.id || @comic.id < Comic.first.id)
             link_to (image_tag "back.png", :title => "Seriously get back to the present"), comic_path(Comic.last)
            else
	      	    link_to (image_tag "back.png", :title => "Back"), comic_path(@comic.previous)
	      	end %>
	      	

<%= unless (params[:id].to_i == 1)
	      		  link_to (image_tag "first.png", :title => "Wait lemme start over"), comic_path(Comic.first)
            end %>
	  			
